# Supabase — for local dev, run `supabase start` and copy values from `supabase status`
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
# Cron: secret for /api/cron/finalize-auctions (finalize ended auctions)
CRON_SECRET=your-cron-secret

# Phone verification: set to "true" when Supabase Auth SMS provider is configured (Twilio, etc.).
# When false, /me shows stub message and user can only save number; badge logic is ready.
NEXT_PUBLIC_PHONE_VERIFICATION_ENABLED=false
